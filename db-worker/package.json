{
  "name": "db-worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "d1:create": "wrangler d1 create smartaibot-db",
    "d1:migrate:db": "wrangler d1 execute smartaibot-db --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
