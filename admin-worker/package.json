{
  "name": "admin-worker",
  "version": "2.0.0",
  "description": "Smart AI Bot Admin Panel — Astro + Vanilla JS on Cloudflare Workers",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.5.4",
    "astro": "^5.6.1"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
